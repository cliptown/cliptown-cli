// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import gleam/option.{type Option, None, Some}

pub type CliEnvValues {
  CliEnvValues(
    cliptown_command: Option(String),
    cliptown_positionals: Option(String),
    reauth_days: String,
    file: Option(String),
    from_clipboard: String,
    pin: String,
    stdin: String,
    limit: String,
    mode: String,
    query: Option(String),
    transport: String,
  )
}

/// Pure: resolve values from an explicit lookup.
pub fn load_from(lookup: fn(String) -> Option(String)) -> CliEnvValues {
  CliEnvValues(
    cliptown_command: nonempty(lookup("CLIPTOWN_COMMAND")),
    cliptown_positionals: nonempty(lookup("CLIPTOWN_POSITIONALS")),
    reauth_days: nonempty_or(lookup("CLIPTOWN_REAUTH_DAYS"), "10"),
    file: nonempty(lookup("CLIPTOWN_FILE")),
    from_clipboard: nonempty_or(lookup("CLIPTOWN_FROM_CLIPBOARD"), "false"),
    pin: nonempty_or(lookup("CLIPTOWN_PIN_CLIP"), "false"),
    stdin: nonempty_or(lookup("CLIPTOWN_STDIN"), "false"),
    limit: nonempty_or(lookup("CLIPTOWN_LIMIT"), "20"),
    mode: nonempty_or(lookup("CLIPTOWN_SEARCH_MODE"), "local_only"),
    query: nonempty(lookup("CLIPTOWN_QUERY")),
    transport: nonempty_or(lookup("CLIPTOWN_PAIR_TRANSPORT"), "wifi"),
  )
}

/// Effectful once the caller supplies process env lookup.
pub fn load_from_os(get_env: fn(String) -> Result(String, Nil)) -> CliEnvValues {
  load_from(fn(key) { result_to_option(get_env(key)) })
}

fn result_to_option(value: Result(String, Nil)) -> Option(String) {
  case value {
    Ok(raw) -> Some(raw)
    Error(_) -> None
  }
}

fn nonempty(raw: Option(String)) -> Option(String) {
  case raw {
    Some("") -> None
    other -> other
  }
}

fn nonempty_or(raw: Option(String), fallback: String) -> String {
  case nonempty(raw) {
    Some(value) -> value
    None -> fallback
  }
}
