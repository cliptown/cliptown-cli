// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class CliEnvValues {
  const CliEnvValues({required this.cliptownCommand, required this.cliptownPositionals, required this.reauthDays, required this.file, required this.fromClipboard, required this.pin, required this.stdin, required this.limit, required this.mode, required this.query, required this.transport});

  final String? cliptownCommand;
  final String? cliptownPositionals;
  final int reauthDays;
  final String? file;
  final bool fromClipboard;
  final bool pin;
  final bool stdin;
  final int limit;
  final String mode;
  final String? query;
  final String transport;
}

/// Pure: resolve values from an explicit lookup.
CliEnvValues loadFrom(String? Function(String key) lookup) {
  return CliEnvValues(
    cliptownCommand: _nonEmpty(lookup('CLIPTOWN_COMMAND')),
    cliptownPositionals: _nonEmpty(lookup('CLIPTOWN_POSITIONALS')),
    reauthDays: _parseInt(lookup('CLIPTOWN_REAUTH_DAYS'), 10),
    file: _nonEmpty(lookup('CLIPTOWN_FILE')),
    fromClipboard: _parseBool(lookup('CLIPTOWN_FROM_CLIPBOARD'), false),
    pin: _parseBool(lookup('CLIPTOWN_PIN_CLIP'), false),
    stdin: _parseBool(lookup('CLIPTOWN_STDIN'), false),
    limit: _parseInt(lookup('CLIPTOWN_LIMIT'), 20),
    mode: _nonEmpty(lookup('CLIPTOWN_SEARCH_MODE')) ?? 'local_only',
    query: _nonEmpty(lookup('CLIPTOWN_QUERY')),
    transport: _nonEmpty(lookup('CLIPTOWN_PAIR_TRANSPORT')) ?? 'wifi',
  );
}

/// Resolve values from a map of env keys.
CliEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
CliEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final cliptownCommand = pick(['CLIPTOWN_COMMAND'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (cliptownCommand != null) out['CLIPTOWN_COMMAND'] = cliptownCommand;
  final cliptownPositionals = pick(['CLIPTOWN_POSITIONALS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (cliptownPositionals != null) out['CLIPTOWN_POSITIONALS'] = cliptownPositionals;
  final reauthDays = pick(['CLIPTOWN_REAUTH_DAYS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '10');
  if (reauthDays != null) out['CLIPTOWN_REAUTH_DAYS'] = reauthDays;
  final file = pick(['CLIPTOWN_FILE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (file != null) out['CLIPTOWN_FILE'] = file;
  final fromClipboard = pick(['CLIPTOWN_FROM_CLIPBOARD'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (fromClipboard != null) out['CLIPTOWN_FROM_CLIPBOARD'] = fromClipboard;
  final pin = pick(['CLIPTOWN_PIN_CLIP'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (pin != null) out['CLIPTOWN_PIN_CLIP'] = pin;
  final stdin = pick(['CLIPTOWN_STDIN'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (stdin != null) out['CLIPTOWN_STDIN'] = stdin;
  final limit = pick(['CLIPTOWN_LIMIT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '20');
  if (limit != null) out['CLIPTOWN_LIMIT'] = limit;
  final mode = pick(['CLIPTOWN_SEARCH_MODE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'local_only');
  if (mode != null) out['CLIPTOWN_SEARCH_MODE'] = mode;
  final query = pick(['CLIPTOWN_QUERY'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (query != null) out['CLIPTOWN_QUERY'] = query;
  final transport = pick(['CLIPTOWN_PAIR_TRANSPORT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'wifi');
  if (transport != null) out['CLIPTOWN_PAIR_TRANSPORT'] = transport;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
